{
    "name": "software-security",
    "version": "0.0.1",
    "description": "Repository di progetto",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "devall": "(cd client && npm run dev) && (cd server && npm run dev)"
    },
    "dependencies": {
        "software-security": "file:",
        "vite": "^6.3.5"
    }
}
